# Engine Metadata

The files in this folder are NexicScript internal metadata files.

- `config.nxs` stores engine configuration.
- `registry.nxs` stores engine registry metadata.
- `templates.nxs` stores template version references.

All engine metadata references the official NexicScript logo at `branding/Logo.png` so the branding stays consistent across the project.
