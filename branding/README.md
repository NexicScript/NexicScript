# NexicScript Brand Assets

This folder contains the official public visuals for NexicScript.

- `Logo.png` is the official organization logo.
- `banner.png` is the public banner used in the README and marketing pages.

The `.nxs` metadata files in this folder are kept for the NexicScript branding system.
